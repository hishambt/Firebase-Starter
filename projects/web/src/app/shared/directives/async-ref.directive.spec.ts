import { AsyncRefDirective } from './async-ref.directive';

describe('AsyncRefDirective', () => {
  it('should create an instance', () => {
    const directive = new AsyncRefDirective();
    expect(directive).toBeTruthy();
  });
});
